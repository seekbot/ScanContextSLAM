# SLAM Practice Workspace in C++
Practicing SLAM and its modules in C++ by building from scratch. 

## Checklists
- **Scan Context** based **Fast-LIO2**: ```scan_context_slam```
- **LTAOM**
Planning more in the future

## CMake
```bash
# Assuming build/ to be created in current directory
cmake -S <pkg_dir> -B build && cmake --build build
```